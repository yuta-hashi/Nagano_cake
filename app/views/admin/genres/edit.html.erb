<h3>ジャンル編集</h3>
<div>
  <%= form_with model:@genre, url: {controller: 'admin/genres', action: 'update'}, local: true do |f| %>
  <label>
    ジャンル名<%= f.text_field :name %>
  </label>
  <%= f.submit "変更を保存" %>
  <% end %>
</div>